# SIAB Intake

Reserved deployable app for future site intake/provisioning workflows.

This app is intentionally not implemented in the first monorepo migration. It
will become its own container/image and Traefik-routed stack after the moved CMS,
public site, template, and orchestrator workflows are stable.

Expected future host: `start.siteinabox.nl`, unless implementation-time route
research picks another host.

