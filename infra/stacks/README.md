# Stack Templates

Stack files under this directory are source templates only until an operator
explicitly syncs them to the VPS.

Do not assume files here are live. The current live paths remain documented in
`infra/README.md`.

