# SIAB Platform Stack Namespace

Target source layout for the future VPS namespace:

```txt
cms/
apps/
  site/
  intake/
tenants/
  amblast/
  ami-care/
```

This directory intentionally starts with documentation only. The first monorepo
move preserves the existing live stack paths and image names.

